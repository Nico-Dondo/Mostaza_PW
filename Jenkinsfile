pipeline {
    agent any
    stages {
        stage('Ejecutar Tests') {
            steps {
                // Usamos la ruta que confirmaste
                bat '"C:\\Program Files\\nodejs\\npx.cmd" playwright test'
            }
        }
    }
}
